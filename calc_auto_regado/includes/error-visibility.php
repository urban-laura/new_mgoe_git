<?php
/*
$error_visibility = '';

if ($c_year != null && $c_month != null)
{
	if ($c_year != $n_year || ($c_year == $n_year && $c_month <= $n_month))
	{
		$error_visibility = 'invisible';
	}
}

if (!isset($_POST['submitted']) || $_POST['fuel'] != 'hibrid' && ($c_year != null && $c_month != null && $c_barrel != null & $c_env != null))
{
	$error_visibility = 'invisible';
}


$output = str_replace('###error-visibility###', $error_visibility, $output); */